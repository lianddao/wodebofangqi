package com.ushareit.listenit;

import android.content.Context;

public class adi {
    public adg m5274a(Context context, adh com_ushareit_listenit_adh) {
        if ((context.checkCallingOrSelfPermission("android.permission.ACCESS_NETWORK_STATE") == 0 ? 1 : null) != null) {
            return new adj(context, com_ushareit_listenit_adh);
        }
        return new ado();
    }
}
